import React from 'react'

const Modal = ({children, 
    isOpen, 
    onClose, 
    title,
    hideHeader,
    showActionBtn,
    actionBtnIcon = null,
    actionBtnText,
    onActionClick,
}) => {
  return <div className=''></div>
  
}

export default Modal